# Warehouse credentials can be imported by specifying the resource identifier.
terraform import lightdash_warehouse_credentials.example "organizations/${organization_uuid}/warehouse-credentials/${organization_warehouse_uuid}"
